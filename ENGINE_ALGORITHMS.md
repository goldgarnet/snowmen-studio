# 충돌·이동 알고리즘 명세

이 문서는 Snowmen Studio의 현재 구현을 기준으로 오브젝트 이동, 충돌, 밀기, 눈덩이 굴림, 힘 전달, 특수 타일 및 턴 종료 처리를 정리한다. 게임 기획의 이상적인 규칙을 별도로 제안하는 문서가 아니라, `src/engine`이 실제로 어떤 순서와 조건으로 상태를 바꾸는지를 설명하는 구현 명세다. 굴림의 내부 tick, 그룹 분리, 애니메이션 프레임 API의 상세는 [TICK_SIMULATION_MECHANICS.md](./TICK_SIMULATION_MECHANICS.md)를 기준으로 한다.

## 1. 관련 코드

| 파일 | 역할 |
| --- | --- |
| `src/engine/turn.ts` | 턴 진입점, 턴 처리 순서, 레이저, 구멍·포털·금 간 타일, 녹기, 영혼 이동, 승패 판정 |
| `src/engine/push.ts` | 플레이어 이동과 밀기 결정표, 연쇄 이동, 눈사람 생성, 간접 힘 전달 |
| `src/engine/roll.ts` | 눈덩이 및 눈덩이 열차의 연속 굴림과 굴림 중 충돌 |
| `src/engine/force.ts` | 벽 등에 눌린 눈덩이의 파괴·분할 처리 |
| `src/engine/helpers.ts` | 한 칸 이동의 지형 통과 판정, 벽 활성 상태, 받침 판정 |
| `src/engine/shadow.ts` | 오브젝트 높이에 따른 그림자 재계산 |
| `src/types.ts` | 레벨·타일·오브젝트 데이터 구조 |
| `src/utils/level.ts` | 좌표, 높이, 경계, 복제 등 기본 유틸리티 |

가장 상위의 호출 흐름은 다음과 같다.

```text
방향 입력
  └─ executeTurn()
       ├─ 레벨 복제
       ├─ executePush()
       │    ├─ 한 칸 이동
       │    ├─ 연쇄 밀기
       │    ├─ rollSnowball()/rollSnowballGroup()
       │    ├─ applyForce()
       │    └─ 눈사람 생성
       ├─ 영혼 발판 → 포털 → 구멍 → 주황 버튼
       ├─ 레이저 → 사망/클리어 판정
       ├─ endOfTurn()
       │    ├─ 금 간 타일 붕괴와 구멍
       │    ├─ 그림자
       │    ├─ 녹기와 소멸
       │    └─ 다음 턴용 상태 기록
       └─ 최종 사망/클리어 판정
```

## 2. 상태 모델과 기본 용어

### 2.1 타일과 오브젝트는 별도 격자다

`Level`은 같은 크기의 두 격자를 가진다.

- `tiles[row][col]`: 바닥, 터널, 아치, 벽 파티션, 버튼, 구멍, 포털 등 지형 상태
- `objects[row][col]`: 플레이어, 눈덩이, 눈사람, 블록, 벽, 나무, 레이저 중 하나 또는 `null`

따라서 포털이나 버튼 같은 타일 위에 오브젝트가 올라갈 수 있다. 반면 일반 오브젝트는 한 칸에 최대 하나만 존재하며, 점유 충돌은 이동 호출자가 따로 검사한다.

### 2.2 오브젝트 종류

| 종류 | 핵심 성질 |
| --- | --- |
| `player` | 입력 주체. 크기 1~3. 눈송이를 먹고 성장하며 더 큰 밀기 힘을 얻는다. |
| `snowball` | 크기 1~2. 조건에 따라 이동, 연속 굴림, 파괴, 분할, 눈사람 생성을 한다. |
| `snowman` | 크기 1~3. 밀릴 수 있고 플레이어 사망 시 영혼이 옮겨갈 후보가 된다. |
| `block` | 단단한 미는 물체. 삼각 블록이면 굴러오는 단일 눈덩이를 반사한다. |
| `wall` | 통과하거나 밀 수 없는 고정 오브젝트다. |
| `tree` | 통과하거나 밀 수 없는 단단한 받침으로 취급된다. |
| `laser` | 블록처럼 밀릴 수 있지만 발사 면에서는 밀 수 없다. 빔을 발사한다. |

### 2.3 크기, 높이, 힘은 서로 다른 개념이다

- `size`는 눈 오브젝트의 크기이자 플레이어의 밀기 힘을 결정하는 값이다.
- 지형 통과에는 `size` 자체가 아니라 `getObjectHeight()`가 쓰이는 경우가 있다.
- 가장자리 아치(`edgeArchTop`, `edgeArchLeft`)는 다시 `size`를 직접 비교한다.

현재 높이 환산은 다음과 같다.

| 오브젝트 | 통과 판정 높이 |
| --- | ---: |
| 플레이어·눈덩이·눈사람 | `size - 0.5` |
| 블록·레이저 | `1.5` |
| 나무 | `treeHeight`, 없으면 `1` |
| 벽 | `0` |

### 2.4 A/B/C/D 표기

`push.ts`는 플레이어가 바라보는 방향의 칸을 다음처럼 부른다.

```text
[P] [A] [B] [C] [D] → 밀기 방향
```

- `P`: 플레이어
- `A`: 플레이어 바로 앞 오브젝트
- `B`, `C`: 그 뒤의 연속 위치
- `D`: 세 개를 한꺼번에 옮길 때 `C`가 들어갈 위치
- `ps`: 플레이어 크기이자 기본 밀기 힘

밀기 규칙은 일반적인 물리 계산식 하나가 아니라, `ps`와 A/B/C 구성에 따른 명시적 결정표로 구현되어 있다. 표에 없는 조합은 기본적으로 움직이지 않는다.

## 3. 한 칸을 통과할 수 있는가

`canMoveTo(level, from, dir, obj)`는 오브젝트가 현재 칸에서 나와 경계를 건너 다음 칸의 지형으로 들어갈 수 있는지만 판정한다.

```text
canLeaveTile(from)
  AND canPassEdge(from → to)
  AND canEnterTile(to)
```

중요하게도 이 함수는 목적지 칸에 다른 오브젝트가 있는지는 보지 않는다. 점유 여부는 `push.ts`, `roll.ts`, `force.ts`가 각 상황에 맞게 처리한다.

### 3.1 맵 경계와 빈 공간

`isInBounds()`는 사각형 배열 범위 안이면서 `isVoid`가 아닌 칸만 유효하다고 본다. 따라서 다음은 모두 맵 바깥과 같은 차단 경계다.

- 행·열 배열 범위 밖
- `isVoid: true`인 칸

### 3.2 기존 터널 타일

터널은 타일 한 칸 자체의 출입 방향과 높이를 제한한다. 출발 칸과 도착 칸 모두 검사된다.

| 타일 | 완전히 막는 방향 | 허용 방향의 높이 제한 |
| --- | --- | --- |
| `isRowArch`(가로 터널) | 좌·우 | 위·아래 이동 시 높이 `≤ 1` |
| `isColumnArch`(세로 터널) | 위·아래 | 좌·우 이동 시 높이 `≤ 1` |

즉 가로 터널은 세로로만, 세로 터널은 가로로만 통과할 수 있으며 허용 방향에서도 낮은 오브젝트만 지나간다.

### 3.3 가장자리 아치

가장자리 아치는 두 칸 사이 경계에 있고 통과 가능한 최대 `size`를 저장한다.

- 값 `1`: 크기 1 이하만 통과
- 값 `2`: 크기 2 이하만 통과
- 값 없음 또는 `0`: 아치 없음

저장은 중복을 피하기 위해 각 타일의 위쪽과 왼쪽 경계에만 한다. 오른쪽 이동은 목적지의 `edgeArchLeft`, 아래 이동은 목적지의 `edgeArchTop`을 읽는다.

눈덩이 열차가 굴러갈 때는 선두만 검사하지 않고 모든 구성원이 자기 위치에서 넘어가는 경계를 각각 검사한다. 눈사람을 조립할 때도 들어가는 작은 눈덩이가 아니라 완성될 눈사람의 크기로 경계를 다시 검사한다.

가장자리 아치는 이동을 막을 수 있지만, 눈사람 조립이나 눈덩이 압착을 위한 단단한 “받침”으로는 인정되지 않는다.

### 3.4 삼각 벽

삼각형의 직각 꼭짓점에 맞닿은 두 변은 막힌 변이고, 나머지 두 변은 열린 변이다.

| `triangle` | 막힌 변 |
| --- | --- |
| `tl` | 위, 왼쪽 |
| `tr` | 위, 오른쪽 |
| `bl` | 아래, 왼쪽 |
| `br` | 아래, 오른쪽 |

출발 타일에서는 나가는 방향의 변을, 도착 타일에서는 진입 방향의 반대편 변을 검사한다. 막힌 변은 일반 이동을 차단하며 단단한 받침으로도 동작한다.

### 3.5 노란 벽과 주황 벽

두 벽은 오브젝트가 아니라 타일 파티션이다.

- 노란 벽: 노란 버튼이 하나 이상 존재하고 모든 노란 버튼이 현재 오브젝트로 덮였을 때만 열린다.
- 주황 벽: 주황 버튼이 하나 이상 존재하고 모든 버튼의 `orangePressed`가 한 번이라도 활성화된 뒤에 영구적으로 열린다.
- 대응 버튼이 하나도 없으면 해당 색 벽은 항상 닫혀 있다.

닫힌 파티션은 진입과 이탈을 모두 막는다. 열린 동안 안으로 들어갔더라도 이후 다시 닫히면 그 안의 오브젝트는 나올 수 없다. 닫힌 파티션은 밀기에서 벽과 같은 충돌물·받침으로 취급되고, 레이저도 막는다.

### 3.6 구멍

- 플레이어는 `canEnterTile()` 단계에서 구멍 진입이 금지된다.
- 다른 오브젝트는 구멍 칸으로 들어갈 수 있고, 이후 구멍 처리 단계에서 사라진다.
- 금 간 타일이 플레이어 밑에서 구멍으로 변하는 식으로 플레이어가 구멍 위에 있게 되면 플레이어 몸은 사라지고 영혼 이전을 시도한다.

## 4. 방향 입력 한 번의 전체 순서

`executeTurn()`은 원본 `Level`을 직접 바꾸지 않고 먼저 깊은 복제한 뒤 복제본을 수정한다.

### 4.1 일반 방향 입력

1. 플레이어가 없으면 즉시 `gameover`를 반환한다.
2. 턴 시작 시 두 포털의 점유 상태를 기록한다.
3. 단조 증가하는 `ageClock`에서 이번 행동의 생성 시각을 받는다.
4. `executePush()`로 이동·밀기·굴림·힘·눈사람 생성을 처리한다.
5. 행동이 성립하지 않았다면 즉시 `playing`을 반환한다. 이때 레이저, 녹기, 금 간 타일 등 턴 종료 효과도 진행되지 않는다.
6. 플레이어가 영혼 이동 발판 위에 있으면 발판을 무장하거나, 이미 무장된 같은 발판이면 영혼을 이전한다.
7. 새로 포털에 들어간 오브젝트를 반대 포털로 옮긴다.
8. 구멍 위의 오브젝트를 제거한다.
9. 덮인 주황 버튼을 영구 활성화한다.
10. 레이저를 발사하고 사라진 오브젝트를 정리한다.
11. 플레이어가 없으면 `gameover`, 활성 목표 위에 있으면 `cleared`를 반환한다.
12. `endOfTurn()`을 실행한다.
13. 녹기·구멍 등 턴 종료 효과 후 다시 사망과 클리어를 판정한다.

`PushResult.playerMoved`라는 이름은 다소 좁다. 실제로는 “이 입력이 유효한 행동으로 해결되었는가”에 가깝다. 눈덩이를 눌러 부수거나 뒤쪽 눈덩이에 힘만 전달한 경우에는 플레이어가 제자리에 있어도 `true`가 되어 나머지 턴 효과가 진행된다.

### 4.2 대기 입력

`executeSkipTurn()`은 이동·포털 후처리 없이 다음 순서로 진행된다.

1. 레벨 복제 및 플레이어 확인
2. 무장된 영혼 이동 발판 해결
3. 레이저 발사
4. `endOfTurn()` 실행
5. 최종 사망·클리어 판정

### 4.3 M 영혼 순환

`cycleSoul()`은 턴을 소비하지 않는 별도 행동이다.

- 읽기 순서(왼쪽 위에서 오른쪽 아래)에서 현재 플레이어 다음에 있는 눈사람으로 이동한다.
- 뒤에 후보가 없으면 첫 눈사람으로 순환한다.
- 원래 몸은 같은 크기와 녹는 상태를 유지한 눈사람이 된다.
- 새 몸은 같은 크기와 녹는 상태를 유지한 플레이어가 된다.
- 이동, 레이저, 녹기, 포털 등 일반 턴 효과는 실행하지 않는다.

### 4.4 화면의 턴 수와 엔진 턴 효과

시뮬레이터와 풀이 기록기는 방향·대기 입력을 받을 때마다 표시용 `turnCount`를 1 증가시킨다. 반면 엔진은 막힌 방향 입력에서 4.1의 5번처럼 턴 종료 효과를 생략한다. 따라서 “입력 횟수”와 “환경 효과가 실제로 진행된 횟수”는 막힌 입력이 있을 때 다를 수 있다.

## 5. 기본 이동과 밀기 결정

### 5.1 이동 전 공통 검사

플레이어 자신이 현재 칸을 나와 A 칸 지형으로 들어갈 수 없으면 전체 입력이 실패한다. A가 레이저이고 플레이어가 레이저의 발사 면 쪽에서 밀려고 해도 실패한다.

예를 들어 레이저가 오른쪽으로 발사한다면 레이저 오른쪽 면이 발사 면이다. 플레이어가 오른쪽에서 왼쪽으로 밀려고 하는 입력은 차단된다. 다른 면에서는 레이저를 블록처럼 밀 수 있다.

A 칸이 비어 있으면 플레이어가 한 칸 이동하고, 도착 칸의 눈송이를 먹을 수 있으면 즉시 먹는다.

### 5.2 물리 통과와 점유 충돌의 우선순위

A가 눈덩이고 플레이어 크기가 눈덩이 이상이며, A와 B 사이에 터널·아치·삼각 벽·닫힌 파티션이 있으면 B의 오브젝트보다 가까운 경계를 먼저 충돌 대상으로 본다. 이 경우 B에 무엇이 있든 A에 직접 힘을 가한다.

이는 `canMoveTo()`가 점유를 무시하기 때문에 별도로 둔 우선순위다. 가까운 벽 너머의 오브젝트가 벽 충돌을 가리는 일을 방지한다.

### 5.3 한 칸, 두 칸, 세 칸 연쇄 이동

실제 이동은 항상 앞쪽 오브젝트부터 뒤에서 앞으로 옮긴다.

- `doMove1`: A→B, P→A
- `doMove2`: B→C, A→B, P→A
- `doMove3`: C→D, B→C, A→B, P→A

각 오브젝트가 새 칸에 도착할 때마다 눈송이 흡수를 처리한다. 이 순서는 먼저 이동한 앞 오브젝트가 목적지를 비워 뒤 오브젝트가 안전하게 들어오게 하며, 중간 상태에서 오브젝트가 덮어써지는 것을 막는다.

### 5.4 플레이어 크기 1 규칙

- A만 있을 때 크기 1 눈덩이, 크기 1 눈사람, 블록·레이저를 한 칸 밀 수 있다.
- 크기 1 눈덩이가 벽 또는 블록·레이저에 눌리면 직접 힘을 받아 눈송이로 부서진다.
- 크기 1 눈덩이 두 개가 연속해 있고 B가 단단히 받쳐져 있으면 크기 1 눈사람을 만든다.
- 그 밖의 연쇄 조합은 움직이지 않는다.

### 5.5 플레이어 크기 2 규칙

A만 있는 경우:

- 크기 1 눈덩이는 한 칸 민 뒤 연속 굴림을 시작한다.
- 크기 2 눈덩이는 한 칸 밀 수 있으며, 못 움직이면 직접 힘을 받아 분할된다.
- 크기 1~2 눈사람과 블록·레이저는 한 칸 밀 수 있다.
- 크기 3 눈사람은 밀 수 없다.

A와 B가 있고 C가 빈 경우:

- A와 B가 모두 크기 1이고 각각 눈덩이·눈사람·블록 계열이면, 둘 다 지형을 통과할 수 있을 때 두 칸 연쇄 이동을 할 수 있다.
- A가 크기 1 눈덩이이고 B가 벽 또는 막힌 블록이면 A를 부순다.
- 크기 1 눈덩이 두 개는 B가 받쳐져 있을 때만 크기 1 눈사람이 된다.
- 크기 2 눈덩이가 벽 또는 막힌 블록에 닿으면 분할된다.
- 크기 2 눈덩이 뒤가 눈덩이·눈사람이면 일반적으로 움직이지 않는다.

A/B/C가 모두 찬 경우:

- A가 크기 1 또는 2 눈덩이고 B가 벽·블록 계열이면 A에 힘을 가한다.
- `눈덩이 1 + 눈덩이 1` 또는 `눈덩이 1 + 눈덩이 2`는 B가 받쳐져 있을 때 각각 크기 1 또는 2 눈사람을 만든다.
- `눈덩이 2 + 눈덩이 1`에서 B가 받쳐져 있으면 A와 플레이어는 움직이지 않고 B에 힘을 가한다.
- 나머지는 움직이지 않는다.

### 5.6 플레이어 크기 3 규칙

A만 있는 경우:

- 눈덩이는 크기와 관계없이 한 칸 민 뒤 굴림을 시작한다. 첫 칸을 통과할 수 없으면 그 자리에서 힘을 받는다.
- 눈사람과 블록·레이저는 한 칸 밀 수 있다.

A와 B가 있고 C가 빈 경우:

- `눈덩이 1 + 눈덩이 1`: 둘 다 전진할 수 있으면 두 눈덩이를 열차로 굴린다. 막히고 B가 받쳐져 있으면 크기 1 눈사람을 만든다.
- `눈덩이 1 + 눈덩이 2`: 둘을 한 칸씩 밀 수 있다. 막히고 B가 받쳐져 있으면 크기 2 눈사람을 만든다.
- `눈덩이 2 + 눈덩이 1`: 둘을 밀 수 있다. 막히고 B가 받쳐져 있으면 B에 힘을 가한다.
- `눈덩이 2 + 눈덩이 2`: B가 받쳐져 있으면 크기 3 눈사람을 만든다.
- 크기 1 눈덩이는 크기 1~2 눈사람을, 크기 2 눈덩이는 크기 1 눈사람을 조건이 맞으면 함께 밀 수 있다.
- 눈덩이 뒤의 블록·레이저도 둘이 모두 통과 가능하면 함께 민다.
- 크기 1 눈사람 또는 블록 계열은 뒤쪽 물체 크기가 2 이하일 때 두 칸 연쇄 이동할 수 있다.
- 크기 2 눈사람은 뒤쪽 물체 크기가 1 이하일 때 두 칸 연쇄 이동할 수 있다.

A/B/C가 모두 찬 경우:

- 세 오브젝트가 모두 크기 1이고 벽이 아니며, A·B가 각 경계를 통과하고 C가 빈 D로 이동할 수 있으면 세 칸 연쇄 이동한다.
- 크기 1 눈덩이 세 개가 이어지고 C가 단단한 물체나 경계에 받쳐진 특수 경우에는 C만 간접 힘을 받아 부서진다. A와 B는 눈사람으로 합쳐지지 않는다.
- 크기 1 눈덩이가 벽에 직접 닿으면 A가 부서진다. B가 블록·레이저인 경우에는 C의 크기가 2 이상인 명시적 조합에서도 A가 부서진다.
- `눈덩이 1 + 눈덩이 1`, `눈덩이 1 + 눈덩이 2`, `눈덩이 2 + 눈덩이 2`는 각각 받침 조건을 충족할 때 크기 1·2·3 눈사람을 만든다.
- `눈덩이 2 + 눈덩이 1`에서 B가 받쳐져 있으면 B에 힘을 가한다.
- 명시된 조건에 들지 않는 조합은 움직이지 않는다.

### 5.7 “받쳐짐” 판정

`isBacked(pos, dir)`는 `pos` 바로 다음 경계나 칸이 압착을 지지하는 단단한 구조인지 판정한다.

받침으로 인정되는 것:

- 맵 경계 또는 void
- 벽, 블록, 나무, 레이저 오브젝트
- 진행 방향을 막는 터널
- 진행 방향을 막는 삼각 벽의 닫힌 변
- 닫힌 노란 벽 또는 주황 벽

받침으로 인정되지 않는 것:

- 눈덩이, 눈사람 등 부드러운 눈 오브젝트
- 가장자리 아치
- 단지 더 먼 칸에 오브젝트가 있다는 사실

## 6. 눈사람 생성

눈사람 생성은 눈덩이에 일반적인 힘을 가하는 것과 별개의 직접 밀기 결과다.

- A와 B의 눈덩이를 제거·병합해 B 위치에 새 눈사람을 둔다.
- 플레이어는 A의 옛 위치로 이동한다.
- 새 눈사람의 `createdAt`은 현재 행동의 단조 증가 시각이다.
- B 타일은 차가운 상태(`isWarm = false`)가 된다.
- 플레이어가 A로 이동한 뒤 A 타일의 눈송이를 먹을 수 있다.
- A→B 경계는 완성될 눈사람 크기로 통과 가능해야 한다. 작은 눈덩이가 낮은 가장자리 아치를 통과한 직후 아치보다 큰 눈사람으로 합쳐지는 것은 금지된다.

일반 생성 조합은 다음과 같다.

| A | B | 결과 |
| --- | --- | --- |
| 눈덩이 1 | 눈덩이 1 | 눈사람 1 |
| 눈덩이 1 | 눈덩이 2 | 눈사람 2 |
| 눈덩이 2 | 눈덩이 2 | 눈사람 3 |

모든 조합은 해당 플레이어 크기 결정표와 B의 받침 조건을 함께 만족해야 한다.

## 7. 직접 힘과 눈덩이 분할

`applyForce()`는 진행 방향으로 더 밀 수 없는 눈덩이를 누를 때 실행된다.

### 7.1 크기 1 눈덩이

- 눈덩이가 사라진다.
- 현재 타일에 눈송이(`isFlake`)가 생긴다.
- 타일은 차가워진다.
- 플레이어는 움직이지 않는다.

### 7.2 크기 2 눈덩이

크기 1 눈덩이 두 개로 나뉘며, 힘의 방향에 수직인 두 방향으로 각각 한 칸 이동을 시도한다.

예를 들어 오른쪽으로 누르면 위와 아래로 갈라진다.

각 절반의 이동 규칙:

1. 터널, 아치, 삼각 벽, 파티션 등 지형을 통과할 수 있어야 한다.
2. 목적지가 비어 있으면 그 칸으로 이동한다.
3. 목적지에 크기 1 이하의 밀 수 있는 오브젝트가 있고, 그 뒤 칸이 비어 있고 통과 가능하면 그 오브젝트를 한 칸 밀고 목적지를 차지한다.
4. 크기 2 이상, 벽, 나무 또는 이동 불가능한 연쇄에 막히면 원점에 남는다.
5. 분할로 미는 동작은 눈사람을 만들지 않는다.

두 절반의 결과:

- 둘 다 이동: 원점은 빈다.
- 하나만 이동: 남은 하나가 원점의 크기 1 눈덩이가 된다.
- 둘 다 못 이동 + 직접 힘: 원점에 크기 1 눈사람이 생긴다.
- 둘 다 못 이동 + 블록이 전달한 간접 힘: 원래 크기 2 눈덩이를 그대로 두고 아무 일도 일어나지 않은 것으로 처리한다.

분할 전체는 하나의 원자적 결과로 취급한다. 특히 원래 눈덩이가 노란 버튼을 누르고 있었다면 두 절반이 모두 이동 가능성을 검사할 때까지 원본 오브젝트를 원점에 유지한다. 첫 절반 처리 직후 버튼이 풀려 노란 벽이 닫히는 순서 의존성을 막기 위한 것이다.

이동하거나 원점에 남은 절반은 해당 칸의 눈송이를 먹을 수 있다.

## 8. 블록을 통한 간접 힘 전달

플레이어가 A의 블록·레이저를 밀 때는 일반 결정표보다 먼저 연속 체인의 막힘을 검사한다.

1. A부터 진행 방향으로 오브젝트가 연속된 위치를 수집한다.
2. 빈 탈출 칸이 있고 체인 길이가 플레이어 크기 이하이면 정상 이동 가능성이 있으므로 간접 힘을 적용하지 않는다.
3. 벽·경계·나무·닫힌 파티션 같은 단단한 끝에 막혔거나, 체인이 플레이어 힘보다 길면 체인이 끼인 것으로 본다.
4. 체인 앞쪽부터 역방향으로 찾으며 단단한 후속 물체에 직접 닿은 가장 앞쪽 눈덩이에 간접 힘을 가한다.
5. 아무 눈덩이도 해당하지 않으면 일반 밀기 결정표로 돌아간다.

간접 힘의 핵심 차이:

- 크기 1 눈덩이는 눈송이로 부서질 수 있다.
- 크기 2 눈덩이는 공간이 있으면 분할될 수 있다.
- 간접 힘만으로는 눈사람을 만들지 않는다.
- 힘이 전달될 뿐 블록과 체인 자체는 이동하지 않는다.

눈덩이끼리는 단단한 후속 물체가 아니다. 예를 들어 `블록-눈덩이1-눈덩이1-벽`에서는 벽에 닿은 마지막 눈덩이만 부서지고 가까운 눈덩이는 남는다.

## 9. 눈덩이 굴림

### 9.1 굴림 시작

굴림은 플레이어가 눈덩이를 먼저 한 칸 밀어 놓은 뒤 시작한다.

- 단일 눈덩이: `rollSnowball()`
- 두 눈덩이가 함께 출발하는 경우: `rollSnowballGroup()`

시작 칸에서도 바로 레이저, 구멍, 포털을 처리한다. 즉 첫 칸이 위험 타일이면 추가로 굴러가기 전에 결과가 발생한다.

### 9.2 매 스텝의 처리 순서

굴림 반복은 대략 다음 순서다.

1. 선두가 삼각 벽 타일에 있고 그룹이 단일 눈덩이면 방향을 90도 반사한다.
2. 그룹의 모든 눈덩이가 각자 현재 경계를 통과할 수 있는지 검사한다.
3. 다음 칸이 비어 있으면 그룹 전체를 한 칸 이동한다.
4. 각 구성원이 도착 칸의 눈송이를 먹고, 그룹의 총질량을 다시 계산한다.
5. 레이저 피격을 검사한다.
6. 선두의 구멍·포털을 처리한다.
7. 다음 칸에 오브젝트가 있으면 충돌 규칙을 적용한다.

맵 구조로 인해 무한 순환할 가능성에 대비해 반복 횟수는 `width × height × 4 + 16`으로 제한된다.

위의 레이저·특수 타일 즉시 처리는 빈 칸으로 이동하거나 삼각 블록에서 반사된 경로에 적용된다. 현재 구현에서는 더 가벼운 장애물 눈덩이 그룹을 밀어 흡수한 분기 직후에는 이 검사를 즉시 호출하지 않는다. 이 분기에서 레이저·구멍·포털 동작을 바꾸려면 별도의 회귀 확인이 필요하다.

### 9.3 눈덩이 열차의 질량 충돌

진행 방향의 다음 칸부터 빈 칸이 나올 때까지 연속된 모든 오브젝트를 장애물 그룹으로 수집한다. 장애물 그룹에 눈덩이가 아닌 것이 하나라도 있으면 굴림은 멈춘다.

모두 눈덩이라면 현재 굴림 그룹과 장애물 그룹의 `size` 합계를 비교한다.

| 비교 | 결과 |
| --- | --- |
| 장애물 총질량 `<` 굴림 총질량 | 장애물 그룹이 한 칸 움직일 공간과 통과 조건이 있으면, 장애물과 굴림 그룹을 모두 한 칸 전진시키고 하나의 굴림 열차로 합친다. |
| 장애물 총질량 `=` 굴림 총질량 | 현재 그룹은 멈추고 장애물 그룹이 같은 방향으로 굴러가기 시작한다. |
| 장애물 총질량 `>` 굴림 총질량 | 현재 그룹이 멈춘다. |

장애물 그룹이 더 가벼워도 그 앞 칸이 차 있거나 구성원 중 하나라도 지형 경계를 통과하지 못하면 양쪽 모두 움직이지 않는다.

눈송이를 먹어 크기가 바뀌면 다음 충돌 전에 총질량을 갱신한다. 선두뿐 아니라 열차의 모든 눈덩이가 자기가 밟은 눈송이를 각각 먹는다.

### 9.4 삼각 반사

삼각 벽의 대각선 반사는 단일 눈덩이에만 적용된다. 둘 이상의 열차는 방향을 꺾지 못한다.

| 삼각형 | 진입 방향 → 반사 방향 |
| --- | --- |
| `br` (`/`) | 아래→왼쪽, 오른쪽→위 |
| `bl` (`\`) | 아래→오른쪽, 왼쪽→위 |
| `tl` (`/`) | 위→오른쪽, 왼쪽→아래 |
| `tr` (`\`) | 위→왼쪽, 오른쪽→아래 |

삼각 블록은 단단한 오브젝트이지만 단일 눈덩이가 열린 대각선 면으로 충돌하면 블록 칸을 통과한 것처럼 반대 출구 칸으로 나가며 반사된다. 다음 조건 중 하나면 멈춘다.

- 닫힌 직각 변 쪽으로 충돌
- 반사 출구 방향의 지형을 통과할 수 없음
- 출구가 맵 밖 또는 void
- 출구 칸이 다른 오브젝트로 점유됨

### 9.5 굴림 중 포털과 구멍

특수 타일 처리는 굴림 그룹의 선두를 기준으로 한다.

- 선두가 구멍에 들어가면 선두가 사라지고 굴림이 종료된다.
- 포털이 정확히 두 개이고 반대편이 비어 있으면 선두가 반대편으로 이동하고 굴림이 종료된다.
- 반대편 포털이 점유됐거나 포털 쌍이 올바르지 않으면 순간이동하지 않고 계속 굴러간다.
- 굴림 중 순간이동한 오브젝트에는 `justTeleported`를 표시해 일반 턴의 포털 후처리에서 다시 순간이동하지 않게 한다.

### 9.6 굴림 중 레이저

각 이동 후 굴림 그룹이 레이저 빔 위에 있는지 확인한다. 피격되면 크기를 0으로 만들고 굴림을 중단한다. 이후 일반 레이저/사망 정리 단계에서 제거된다.

굴림 중 사용하는 즉시 피격 검사는 전체 턴의 레이저 처리와 목적이 다르다. 빔 경로에서 처음 만난 오브젝트까지만 확인하고, 굴림 그룹의 구성원일 때만 제거 대상으로 표시한다. 또한 현재 이 즉시 검사에서는 닫힌 노란 벽은 빔을 막지만 닫힌 주황 벽은 검사하지 않는다. 턴 후반의 전체 레이저 검사는 노란 벽과 주황 벽을 모두 차단물로 처리한다.

## 10. 눈송이 흡수

오브젝트가 일반 이동, 연쇄 밀기, 분할, 굴림으로 눈송이 타일에 도착하면 다음 상한까지 크기가 1 증가한다.

| 오브젝트 | 최대 크기 |
| --- | ---: |
| 플레이어 | 3 |
| 눈덩이 | 2 |
| 눈사람 | 3 |

성장한 경우 눈송이는 사라지고 타일은 차가워진다. 이미 최대 크기이거나 블록·나무·레이저 같은 비눈 오브젝트라면 눈송이를 소비하지 않는다.

## 11. 포털, 버튼, 금 간 타일

### 11.1 일반 이동 후 포털

일반 이동에서는 턴 시작 때 각 포털의 점유 여부를 기억한다. 이동 후 다음을 만족하는 오브젝트만 반대 포털로 옮긴다.

- 포털이 정확히 두 개다.
- 턴 시작 시 해당 포털 칸이 비어 있었고, 이후 새 오브젝트가 들어왔다.
- 굴림 중 이미 순간이동하지 않았다.
- 반대편 포털이 비어 있다.

점유 스냅샷은 오브젝트 신원이 아니라 포털 칸별 불리언 값이다. 따라서 턴 시작에 차 있던 포털이 도중에 비고 다른 오브젝트가 들어와도 그 포털은 이번 후처리에서 건너뛴다. `justTeleported`는 턴 종료에 초기화된다.

### 11.2 버튼

- 초록 버튼(`isKeyTile`): 하나라도 덮이지 않았으면 목표가 비활성화된다. 모든 초록 버튼이 덮여야 클리어할 수 있다.
- 노란 버튼: 모든 버튼의 현재 점유 상태가 노란 벽의 열림 여부를 실시간으로 결정한다.
- 주황 버튼: 오브젝트가 한 번 덮으면 `orangePressed = true`가 되고 되돌아가지 않는다. 모든 주황 버튼이 누적 활성화되면 주황 벽이 영구적으로 열린다.

### 11.3 금 간 타일

턴 종료 시 현재 오브젝트로 덮인 금 간 타일은 `crackArmed` 상태가 된다. 다음으로 턴 종료 효과가 실제 진행되는 행동에서 다음 순서를 따른다.

1. 이전에 무장된 금 간 타일이 구멍으로 변한다.
2. 그 위에 아직 오브젝트가 있으면 즉시 빠진다.
3. 이번 처리 후 새로 덮인 금 간 타일을 다음 턴용으로 무장한다.

막힌 방향 입력은 턴 종료 효과를 실행하지 않으므로 금 간 타일도 붕괴시키지 않는다. 대기 입력은 턴 종료 효과를 실행한다.

## 12. 레이저

레이저 빔은 `laserDirection` 방향으로 한 칸씩 전진한다.

빔을 즉시 막는 것:

- 맵 경계와 void
- 벽, 블록, 나무, 다른 레이저
- 닫힌 노란 벽과 주황 벽
- 삼각 벽 타일. 빔은 해당 타일까지 도달한 뒤 더 진행하지 않는다.

플레이어, 눈덩이, 눈사람 등 차단 목록에 없는 오브젝트는 크기가 0이 되어 제거된다. 플레이어가 제거되면 가장 가까운 눈사람으로 영혼 이전을 시도한다.

터널과 가장자리 아치는 레이저 빔을 막지 않는다.

일반 방향 턴에서는 레이저가 목표 판정보다 먼저 발사되므로, 목표 타일 위에 도착해도 빔에 맞으면 클리어보다 사망이 우선한다.

## 13. 턴 종료: 그림자와 녹기

`endOfTurn()`의 순서는 다음과 같다.

1. 이전에 무장된 금 간 타일을 구멍으로 바꾸고 낙하 처리
2. 그림자 기능이 켜져 있으면 그림자 재계산
3. 녹기 처리
4. 크기 0 이하 오브젝트 제거 및 영혼 이전
5. 크기가 바뀌었다면 그림자 재계산
6. 현재 덮인 금 간 타일 무장
7. 주황 버튼 래치
8. 포털 중복 이동 방지 플래그 초기화

### 13.1 그림자

- 햇빛 반대 방향으로 오브젝트 높이의 내림값만큼 그림자를 드리운다.
- 터널 타일은 기본적으로 그늘이다.
- 닫힌 노란·주황 벽 타일 내부는 항상 그늘이다.
- 닫힌 벽 안의 오브젝트 그림자는 바깥으로 나오지 않는다.
- void에는 그늘을 설정하지 않는다.

### 13.2 녹기

가열 조건은 `tile.isWarm && !tile.isShade`다.

- 플레이어: 처음 가열된 턴에는 `isMelting = true`만 된다. 다음 연속 가열 턴부터 크기가 1씩 감소한다. 차갑거나 그늘진 곳에 가면 `isMelting`이 해제된다.
- 눈덩이·눈사람: 가열된 매 턴 크기가 즉시 1씩 감소한다.
- 블록, 벽, 나무, 레이저: 녹지 않는다.
- 크기가 0 이하가 되어 완전히 사라질 때만 그 타일이 차가워진다.

## 14. 사망과 영혼 이동

플레이어 몸이 레이저, 녹기, 구멍 때문에 사라지면 모든 눈사람 중 새 몸을 고른다.

1. 사라진 위치와의 유클리드 거리가 가장 가까운 눈사람
2. 거리가 같으면 `createdAt`이 더 작은, 즉 더 오래된 눈사람

선택된 눈사람은 같은 크기와 녹는 상태를 보존한 플레이어가 된다. 후보 눈사람이 없으면 플레이어가 없는 상태가 유지되고 이후 `gameover`가 된다.

영혼 이동 발판도 가장 가까운 눈사람과 오래된 순서 규칙을 사용하지만, 기존 플레이어 몸을 눈사람으로 남긴다는 차이가 있다. 발판은 밟자마자 발동하지 않고 같은 발판 위에서 다음 유효 턴 또는 대기까지 머물러야 발동한다.

`createdAt`에는 `Date.now()`가 아니라 프로세스 내 단조 증가 카운터를 사용한다. 빠른 풀이 재생에서도 생성 순서가 같아 영혼 이전 결과가 결정적으로 재현되게 하기 위해서다.

## 15. 클리어 판정

클리어 조건은 다음 두 가지를 모두 만족하는 것이다.

- 현재 영혼을 가진 플레이어가 목표 타일 위에 있다.
- 초록 버튼이 없다면 즉시 활성, 있다면 모든 초록 버튼이 오브젝트로 덮여 있다.

일반 방향 턴은 레이저 직후 한 번, 전체 턴 종료 효과 후 한 번 판정한다. 첫 판정에서 이미 클리어되면 턴 종료 효과는 실행하지 않고 즉시 반환한다. M 영혼 순환은 호출자가 `isLevelCleared()`로 즉시 판정한다.

## 16. 구현상 불변 조건과 수정 시 주의점

### 16.1 지형 판정과 오브젝트 충돌을 섞지 않는다

`canMoveTo()`는 지형 경계만 판단한다. 목적지 점유를 추가로 검사하도록 바꾸면 연쇄 밀기, 눈사람 생성, 굴림 충돌처럼 일부러 점유 칸을 향해 진행하는 로직이 깨질 수 있다.

### 16.2 연쇄 이동은 앞에서부터 옮긴다

두 개 이상을 미는 경우 가장 먼 오브젝트부터 이동해야 한다. P나 A부터 옮기면 아직 점유 중인 칸을 덮어쓸 수 있다.

### 16.3 버튼 상태는 처리 중간에도 의미가 있다

노란 벽은 현재 점유 상태를 즉시 다시 계산한다. 특히 분할, 굴림, 연쇄 이동에서 버튼을 언제 비우는지가 이후 구성원의 통과 가능성을 바꾼다.

### 16.4 받침과 단순 이동 차단은 다르다

가장자리 아치는 이동을 막지만 받침은 아니다. 반대로 맵 경계, 단단한 오브젝트, 닫힌 파티션, 터널, 삼각 벽의 닫힌 변은 힘 전달과 눈사람 조립의 받침이 될 수 있다.

### 16.5 굴림 열차는 모든 구성원을 검사한다

선두만 지형을 검사하면 뒤의 큰 눈덩이가 낮은 아치를 통과하는 오류가 생긴다. 모든 구성원이 자기 경계에서 `canMoveTo()`를 통과해야 한다.

### 16.6 실패 입력과 유효한 제자리 행동을 구분한다

- 실패 입력: `playerMoved = false`, 턴 종료 효과 없음
- 눈덩이 파괴·분할·간접 힘: 플레이어는 제자리지만 `playerMoved = true`, 턴 종료 효과 있음

새 충돌 규칙을 추가할 때 어느 쪽인지 명확히 해야 레이저, 녹기, 금 간 타일의 시간 진행이 일관된다.

## 17. 변경 시 확인할 회귀 시나리오

충돌·이동 코드를 바꿀 때는 최소한 다음 조합을 함께 확인하는 것이 좋다.

- 플레이어 크기 1·2·3 각각의 A 단독, A+B, A+B+C 밀기
- 맵 끝, void, 터널, 가장자리 아치, 삼각 벽, 닫힌 색상 벽 앞의 눈덩이
- 낮은 아치를 사이에 둔 눈사람 생성과 크기가 다른 굴림 열차
- 크기 1 눈덩이 파괴, 크기 2 눈덩이 양방향·한쪽·완전 차단 분할
- 노란 버튼 위에서 크기 2 눈덩이 분할
- 블록-눈덩이-벽 및 플레이어 힘보다 긴 블록 연쇄
- 굴림 그룹의 더 작은·같은·더 큰 질량 충돌
- 굴림 중 눈송이 흡수 후 질량 재계산
- 삼각 벽과 삼각 블록의 네 방향 반사
- 포털 도착, 막힌 반대 포털, 굴림 중 포털, 구멍 진입
- 목표 위 레이저, 금 간 타일 붕괴, 녹은 플레이어의 영혼 이전
- 같은 거리의 여러 눈사람 사이에서 오래된 몸을 고르는 결정성

이 문서와 구현이 다를 경우 현재 실행 결과의 기준은 `src/engine` 코드다. 규칙을 변경할 때는 코드와 이 문서를 같은 작업에서 함께 갱신해야 한다.
