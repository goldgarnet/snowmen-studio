# dependencies
node_modules

# design reference (not part of the app)
Snowmen Studio.dc.html
support.js

# build output
dist
dist-ssr

# local env (contains Supabase keys — never commit)
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
node_modules/.tmp
